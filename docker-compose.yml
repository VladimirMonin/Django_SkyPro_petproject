version: '3.8'
services:
   full_django_app:
       build: .
       volumes:
         - ./:/app
       command: [ "python", "manage.py", "runserver", "0.0.0.0:8000" ]
       ports:
           - '8000:8000'